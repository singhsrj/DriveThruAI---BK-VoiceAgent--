# Standard Postgres connection string (Neon, RDS, local, etc).
# postgres:// and postgresql:// are both fine -- normalized internally.
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require
